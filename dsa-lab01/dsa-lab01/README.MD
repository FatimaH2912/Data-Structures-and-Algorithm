# DSA Lab 01

**Name:** Fatima Haroon
**Registration Number:** 577112

## Task 1

In this task I created an integer array and then changed the value of third index. Then I used a for loop to print out all the elements in the array with spaces between them. The predicted output was: `2 4 7 8 10`.

## Task 2

In this task I took input of five numbers from the users using a for loop and then using another for loop to add all the elements in the array in a variable total then printed out the sum.

## Task 3 

In this task I created a class based on OOP principles named Student and then assigned it two integer attributes and a function display to print the user data. Then I created two objects, entered their data and printed them out. Then I changed s1 marks and printed both the student’s data again.

### Task 3 Test Results

1. **Initial values:**
   s1: Roll Number = 1, Marks = 75
   s2: Roll Number = 2, Marks = 90

2. **After changing s1 marks:**
   s1: Roll Number = 1, Marks = 80
   s2: Roll Number = 2, Marks = 90

3. **Final result:**
   Only s1 marks changed from 75 to 80, while s2 marks remained 90 because both objects have their own separate data.

## Task 4

First I created an array of 8 integers then using for loop took an input of 8 integers. Then I assigned four variables each assuming the first index is the answer then using the for loop I compared i+1 to i index element to the end of the list then printed out the data.

## Task 5

In this task I reversed an array using recursion. First I made a function that ensures the starting index is less than last index. If that condition is met the function continues and using the temporary variable method it swaps the first and last element and then the function calls itself again and again till the start and last index are same. Then using for loop I took an input of six integers and then called the reverseArray function and then printed out the reversed array.

## Task 6

In this task we pick out the unique integers in the array and keep them in the same order. So in this task I first take an input of 10 integers using for loop then the array remains same but we check every integer to ensure only unique remain. The first loop runs for the first element and then the second one checks the rest of the elements with the first. If it matches the loop breaks and the next element’s matching starts and if the Boolean value remains false the elements keep getting added to the next position till the end of the array.
